name: Publish queued blog post

on:
  schedule:
    - cron: '0 8 * * 2,4,6' # Tuesdays, Thursdays, and Saturdays at 8am UTC
  workflow_dispatch:         # Manual trigger from GitHub Actions tab

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --ignore-scripts --no-audit --no-fund

      - name: Validate content and publisher
        run: npm run content:check && npm run test:publisher

      - name: Publish next queued post
        run: node scripts/publish-next.mjs

      - name: Check for changes
        id: changes
        run: |
          git diff --quiet && echo "changed=false" >> $GITHUB_OUTPUT || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Validate published output
        if: steps.changes.outputs.changed == 'true'
        run: npm run links:check && npm run build

      - name: Commit and push if changed
        if: steps.changes.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "blog: publish queued post

          Auto-published the next queued blog post and regenerated blog + sitemap."
          git push
